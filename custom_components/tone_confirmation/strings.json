{
  "config": {
    "abort": {
      "already_configured": "This conversation agent is already wrapped."
    },
    "error": {
      "already_configured": "This conversation agent is already wrapped by another entry."
    },
    "step": {
      "user": {
        "description": "Choose a conversation agent to wrap. Add the integration again to create wrappers for other agents. The integration plays Home Assistant's standard confirmation tone after successful actions.",
        "data": {
          "target_agent": "Conversation agent"
        },
        "data_description": {
          "target_agent": "The underlying agent that processes requests."
        }
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "data": {
          "target_agent": "Conversation agent"
        },
        "data_description": {
          "target_agent": "The underlying agent that processes requests."
        }
      }
    }
  }
}
